<%= @user.name %>
<br/>
<%= link_to 'Edit Profile', edit_user_path(@user) if @user == current_user %>
<%= link_to 'Delete', user_path(@user), method: :delete, data: { confirm: "Are you sure?" } if @user == current_user %>

